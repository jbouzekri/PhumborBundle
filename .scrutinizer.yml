checks:
    php: true

tools:
    external_code_coverage:
        timeout: 600    # Timeout in seconds.

filter:
    excluded_paths:
        - tests/*
        - Tests/*
        - spec/*
        - unit/*
        - src/Jb/Bundle/PhumborBundle/Tests/*
